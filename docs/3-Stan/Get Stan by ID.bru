meta {
  name: Get Stan by ID
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api/superadmin/stan/1
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response success is true", function() {
    expect(res.getBody().success).to.equal(true);
  });
  
  test("Response data has nama_stan", function() {
    expect(res.getBody().data.nama_stan).to.exist;
  });
}
